int main() 
{
    int n;
    scanf("%d", &n);
        int num1,num2, sum = 0;
        for (int i = 0; i < n; ++i) 
        {
            scanf("%d%d", &num1,&num2);
            sum = num1 + num2;
            printf("%d\n", sum);
        }
    return 0;
}
